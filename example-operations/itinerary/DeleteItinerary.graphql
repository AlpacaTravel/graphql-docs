# Removes an itinerary.

mutation deleteItinerary($id: ID!) {
  # Use the deleteItinerary operation to remove an itinerary
  deleteItinerary(
    # Supply your ID of the itinerary to remove (no undo)
    id: $id
  ) {
    # Select back the ID of the itinerary that was removed
    id
  }
}
